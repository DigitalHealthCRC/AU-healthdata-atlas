[project]
name = "au-healthdata-map"
version = "0.1.0"
description = "Curated Australian health data access pathway register, Neo4j knowledge graph loader, and map payload exporter."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "neo4j>=5.0",
]

[tool.uv]
package = false
